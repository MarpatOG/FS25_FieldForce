# Sample Profiles

Sample profiles are not required for MVP operation. The app ships built-in wheel profiles in `WheelProfileCatalog`, and user effect profiles are generated at runtime under `%APPDATA%/FieldForce/effect-profiles/`.
